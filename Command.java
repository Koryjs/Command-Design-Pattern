/**
 * @author Kory Singleton
 * An interface to assist with executing the in game commands
 */
public interface Command {
    /**
     * Executes the given command in game
     */
    public void execute();
}
